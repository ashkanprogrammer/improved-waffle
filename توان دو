#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    long long int c=0;
    long long int n;
    cin>>n;
    for(long long int i=0;i<=n;i++)
    {
        c=pow(2,i);
        if(c>n)
        {
            cout<<c;
            break;
        }
    }
}
